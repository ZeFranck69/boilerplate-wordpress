{% extends 'base.twig' %}

{% block content %}

{% if sections|length == 0 %}
    {{ post.content }}
{% else %}
    {% for section in sections %}
        {% include 'flexibles/' ~ section.acf_fc_layout ~ '.twig' %}
    {% endfor %}
{% endif %}

{% endblock %}